app {
  // Integration tests drive the passthrough over HTTP as well as through the service seam.
  allow-sparql-passthrough = true

  triplestore {
    dbtype = "fuseki"

    fuseki {
      port = 3030
      username = "admin"
      password = "test"
    }

    reload-on-start = false

    is-test-env = true
  }

  features {
    allow-erase-projects = true
    allow-project-data-import = true
  }
}
