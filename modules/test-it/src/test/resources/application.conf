include "test"

app {
  triplestore {
    dbtype = "fuseki"

    fuseki {
      port = 3030
      username = "admin"
      password = "test"
    }

    reload-on-start = false

    is-test-env = true
    cookie-domain = "0.0.0.0"
  }

  knora-api.external-host = "0.0.0.0"

  features {
    allow-erase-projects = true
  }
}
