include "application"
app {
    triplestore {
        dbtype = "fuseki"

        fuseki {
            port = 8080
            repository-name = "knora-test-unit"
            tomcat = true
            tomcat-context = "fuseki"
        }

        reload-on-start = false
    }
}