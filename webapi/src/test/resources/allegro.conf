include "application.conf"

app {
    triplestore {
        dbtype = "allegro"

        allegro {
            port = 10035
            repository-name = "knora-test-unit"
            username = "test"
            password = "xyzzy"
        }

        reload-on-start = false
    }
}