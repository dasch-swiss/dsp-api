include "application"
app {
    triplestore {
        dbtype = "fuseki"

        fuseki {
            port = 3030
            repository-name = "knora-test-unit"
        }

        reload-on-start = false
    }
}