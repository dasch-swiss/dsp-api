include "application.conf"

app {
    triplestore {
        dbtype = "virtuoso"

        virtuoso {
            port = 8890
            username = "dba"
            password = "dba"
        }

        reload-on-start = false
    }
}