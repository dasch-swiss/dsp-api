syntax = "proto3";

package proto; // Requried to generate valid code.

// Always import protos with a full path relative to the WORKSPACE file.
import "proto/zip_code.proto";

message Address {
  string city = 1;
  ZipCode zip_code = 2;
}
