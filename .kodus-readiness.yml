criteria:
  # Kodus does not recognise Scala tooling — supplemental assessment script handles
  # linter, formatter, type-checker, test-framework, test-files-exist, ci-runs-linters.
  # This criterion genuinely cannot be evaluated from the local filesystem:
  bundle-analysis: false   # N/A — Scala/JVM backend, no frontend bundle
