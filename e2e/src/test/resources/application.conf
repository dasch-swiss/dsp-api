app {
    allow-reload-over-http = true
    triplestore {
        fuseki {
            port = 3030
            repository-name = "dsp-e2e"
            username = "admin"
            password = "test"
        }
        reload-on-start = false
        auto-init = true
    }
}
